{{- include "ffc-helm-library.container-config-map" (list . "coreai-mcu-persona.config-map") -}}
{{- define "coreai-mcu-persona.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
